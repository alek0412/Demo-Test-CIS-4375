# Server
PORT=3000

# Flask (Python) on 3001 — Node proxies customer/waiver/employee/court APIs here (same paths as [Python]/routes).
# Default if unset: http://127.0.0.1:3001
# FLASK_API_URL=http://127.0.0.1:3001
# FLASK_API_URL=   (empty value = disable Flask; Node-only customer preview, no waiver proxy)
# (legacy) FLASK_WAIVER_URL=http://127.0.0.1:3001

# Database (AWS RDS)
DB_HOST=reservation-capstone-db.czltypivanye.us-east-1.rds.amazonaws.com
DB_PORT=3306
DB_NAME=reservation_db
DB_USER=your_db_username
DB_PASSWORD=your_db_password

# Optional: full MySQL URL (overrides individual DB_* vars if set)
# DATABASE_URL=mysql://user:password@reservation-capstone-db.czltypivanye.us-east-1.rds.amazonaws.com:3306/reservation_db

# Admin (optional; defaults in server.js)
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=Admin123!

# Customer login
# Preview (default): env-only, no DB — good for local UI preview. Set to 0 for real DB bcrypt auth.
# CUSTOMER_PREVIEW_LOGIN=1
# CUSTOMER_EMAIL=your@email.com
# CUSTOMER_PASSWORD=your_password
